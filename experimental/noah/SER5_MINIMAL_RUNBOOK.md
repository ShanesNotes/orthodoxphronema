# Noah SER5 Minimal Runbook

> Legacy alias. The active generic runbook is
> `experimental/noah/MINIMAL_RUNTIME_RUNBOOK.md`.

This file is retained because earlier experimental notes and handoffs point here.

Use the generic runbook for the active path:

- `git pull` first
- official pericope queue as the traversal source
- local `bible/` scripture mirror in the vault
- local prompts, journals, and state only

If a concrete single-machine implementation is needed later, derive it from the generic
runbook rather than treating this filename as a separate contract.
