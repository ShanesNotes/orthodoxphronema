# Canon Directory — IMMUTABLE SUBSTRATE

This directory contains promoted, audited, ratified scripture files.

## CRITICAL
- These files are READ-ONLY to all agents.
- Modifications happen ONLY via `pipeline/promote/promote_book.py`.
- Manual edits are FORBIDDEN — no exceptions.
- Format: one-verse-per-line, YAML frontmatter, `## Chapter N` headers.
- No commentary, no study articles, no footnotes inline.

If you need to fix something in a canon file, the fix must go through:
staged file → validation → Ezra audit → Human ratification → promote script
